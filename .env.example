# =================================
# BARBERÍA ELITE - PRODUCCIÓN
# CONFIGURACIÓN WINDOWS
# =================================

# Database (MySQL local o servicio en la nube)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password_mysql
DB_NAME=barberia_prod
DB_PORT=3306

# JWT Secret (se generará automáticamente)
JWT_SECRET=tu-jwt-super-secreto-aqui-se-generara-automaticamente

# Admin Credentials (CAMBIAR EN PRODUCCIÓN!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin123!

# Security
NODE_ENV=production
PORT=5000

# CORS (URLs de frontend)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://tu-app-barberia.netlify.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100